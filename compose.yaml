services:
  ngrok:
    image: ngrok/ngrok:alpine
    command: http --url=tinkly-catchable-aurea.ngrok-free.dev host.docker.internal:8081
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN}
    ports:
      - "4040:4040"
    extra_hosts:
      - "host.docker.internal:host-gateway"

